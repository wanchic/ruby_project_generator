#!/usr/bin/env ruby

#$LOAD_PATH << File.expand_path(File.join(['..', 'lib']), __dir__ )

require 'rubygems'

#Dir[File.expand_path(File.join(['..', 'lib','*.rb']), __dir__ )].sort.each{|f| require f}

# If you need Command Arguments
# p_name = CmdArguments.program_name ARGV
